{
  "name": "inventory-list",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "all": "npm setup && npm start",
    "setup": "npm install && cd api && npm install && cd ../app && npm install",
    "start": "concurrently --kill-others-on-fail \"cd api && npm start\" \"cd app && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
